# Backend API URL — leave unset for local development (defaults to
# http://localhost:5000). Set this to your deployed backend's URL when
# you deploy the frontend, e.g.:
#   VITE_API_URL=https://vexa-backend.onrender.com
# No trailing slash.
VITE_API_URL=
